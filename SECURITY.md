# 安全策略

air780e-hub 会处理短信正文、验证码、电话号码、设备标识和第三方推送凭据。请把它作为敏感业务系统部署，而不是普通的无状态 Web 应用。

## 支持范围

安全修复以 `master` 分支最新版本为准。当前不维护旧版本分支。

## 报告漏洞

请使用 GitHub 仓库 Security 页面中的 **Report a vulnerability** 私密报告入口。不要在公开 Issue、Discussion、Pull Request、截图或日志中披露以下内容：

- 真实短信或验证码；
- 电话号码、IMEI、ICCID、SMSC；
- Agent Token、管理员会话 Cookie；
- Bark Key、Telegram Bot Token、Webhook、SMTP 密码；
- 数据库、备份或包含上述信息的日志。

报告中请提供可最小化复现的信息；所有标识符和载荷都应替换为虚构值。

## 部署责任

维护者建议至少满足以下基线：

1. Web 管理端只通过 HTTPS 访问，Agent 只使用 WSS；
2. 明文 HTTP 端口仅监听回环地址或受防火墙保护的私有网络；
3. Agent 配置权限为 `0600`，仅服务账户可读；
4. Server 数据卷和备份加密、限权并定期测试恢复；
5. 管理员密码和 Agent Token 使用独立高强度值；
6. 推送渠道使用专用、可撤销的凭据；
7. 操作系统、Docker、Python 依赖和反向代理及时更新；
8. 不向公开监控或日志平台发送短信正文。

## 凭据泄露处理

如果凭据可能已经泄露：

1. 立即停止公开访问或限制入口；
2. 轮换 Agent Token，并更新全部 Agent；
3. 轮换受影响的推送渠道、SMTP 和机器人凭据；
4. 清除管理员会话，修改管理员密码；
5. 检查访问日志和通知日志中的异常活动；
6. 如果敏感值曾进入 Git，除修改当前文件外还必须清理历史并强制更新远程引用。

删除一条提交或把文件加入 `.gitignore` 不能从 Git 历史中移除已经提交的秘密。